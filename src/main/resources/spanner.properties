project.id=your-project-id
instance.id=your-instance-id
database.id=your-database-id

# Performance tuning parameters
min.sessions=10
max.sessions=100
num.channels=8
write.sessions.fraction=0.8
prefetch.chunks=4

# Query optimization
allow.partition.scan=true
optimizer.version=2
optimizer.statistics.package=latest

# Batching configuration
rpc.priority=HIGH
commit.deadline.seconds=60
 